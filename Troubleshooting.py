TROUBLESHOOTING

SSL Certificate Errors: If you encounter certificate errors, the script uses -k with curl to bypass SSL verification. Remove this for more secure requests.

Permission Denied: Make sure the script is executable (chmod +x quote.sh).

Error Posting Quote: Verify your PAGE_ID and ACCESS_TOKEN in config.sh.
